apiVersion: v1
kind: Service
metadata:
  name: llmmll-composer
  namespace: llmmll
  labels:
    app: llmmll-composer
spec:
  type: ClusterIP
  selector:
    app: llmmll-composer
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
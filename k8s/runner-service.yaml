apiVersion: v1
kind: Service
metadata:
  name: llmmll-runner
  namespace: llmmll
  labels:
    app: llmmll-runner
spec:
  type: ClusterIP
  selector:
    app: llmmll-runner
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
      protocol: TCP
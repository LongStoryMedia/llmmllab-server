# Code generated by schema2code at 2026-02-23 18:45:11. DO NOT EDIT.


from __future__ import annotations
from typing import List, Dict, Optional, Any, Union, Annotated, Literal
from datetime import datetime, date, time, timedelta
from models.openai.chat_completion_tool import ChatCompletionTool
from models.openai.easy_input_message import EasyInputMessage
from models.openai.eval_item import EvalItem
from models.openai.eval_item_content import EvalItemContent
from models.openai.eval_item_content_array import EvalItemContentArray
from models.openai.eval_item_content_item import EvalItemContentItem
from models.openai.eval_item_content_output_text import EvalItemContentOutputText
from models.openai.eval_item_content_text import EvalItemContentText
from models.openai.eval_item_input_image import EvalItemInputImage
from models.openai.eval_jsonl_file_content_source import EvalJsonlFileContentSource
from models.openai.eval_jsonl_file_id_source import EvalJsonlFileIdSource
from models.openai.eval_stored_completions_source import EvalStoredCompletionsSource
from models.openai.function_object import FunctionObject
from models.openai.function_parameters import FunctionParameters
from models.openai.image_detail import ImageDetail
from models.openai.input_audio import InputAudio
from models.openai.input_content import InputContent
from models.openai.input_file_content import InputFileContent
from models.openai.input_image_content import InputImageContent
from models.openai.input_message_content_list import InputMessageContentList
from models.openai.input_text_content import InputTextContent
from models.openai.metadata import Metadata
from models.openai.reasoning_effort import ReasoningEffort
from models.openai.response_format_json_object import ResponseFormatJsonObject
from models.openai.response_format_json_schema import ResponseFormatJsonSchema
from models.openai.response_format_json_schema_schema import (
    ResponseFormatJsonSchemaSchema,
)
from models.openai.response_format_text import ResponseFormatText
from pydantic import BaseModel, ConfigDict, Field, AnyUrl, EmailStr, conint, confloat


class TemplateInputMessages(BaseModel):
    template: Annotated[
        List[Union[EasyInputMessage, EvalItem]],
        Field(
            ...,
            description="A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}.",
        ),
    ]
    """A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}."""
    type: Annotated[
        Literal["template"],
        Field(..., description="The type of input messages. Always `template`."),
    ]
    """The type of input messages. Always `template`."""

    model_config = ConfigDict(extra="ignore")


class ItemReferenceInputMessages(BaseModel):
    item_reference: Annotated[
        str,
        Field(
            ...,
            description='A reference to a variable in the `item` namespace. Ie, "item.input_trajectory"',
        ),
    ]
    """A reference to a variable in the `item` namespace. Ie, \"item.input_trajectory\""""
    type: Annotated[
        Literal["item_reference"],
        Field(..., description="The type of input messages. Always `item_reference`."),
    ]
    """The type of input messages. Always `item_reference`."""

    model_config = ConfigDict(extra="ignore")


class SamplingParams(BaseModel):
    max_completion_tokens: Annotated[
        Optional[int],
        Field(
            default=None,
            description="The maximum number of tokens in the generated output.",
        ),
    ] = None
    """The maximum number of tokens in the generated output."""
    reasoning_effort: Annotated[Optional[ReasoningEffort], Field(default=None)] = None
    response_format: Annotated[
        Optional[
            Union[
                ResponseFormatText, ResponseFormatJsonSchema, ResponseFormatJsonObject
            ]
        ],
        Field(
            default=None,
            description='An object specifying the format that the model must output.  Setting to `{ "type": "json_schema", "json_schema": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).  Setting to `{ "type": "json_object" }` enables the older JSON mode, which ensures the message the model generates is valid JSON. Using `json_schema` is preferred for models that support it. ',
        ),
    ] = None
    """An object specifying the format that the model must output.  Setting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs).  Setting to `{ \"type\": \"json_object\" }` enables the older JSON mode, which ensures the message the model generates is valid JSON. Using `json_schema` is preferred for models that support it. """
    seed: Annotated[
        Optional[int],
        Field(
            default=42,
            description="A seed value to initialize the randomness, during sampling.",
        ),
    ] = 42
    """A seed value to initialize the randomness, during sampling."""
    temperature: Annotated[
        Optional[float],
        Field(
            default=1,
            description="A higher temperature increases randomness in the outputs.",
        ),
    ] = 1
    """A higher temperature increases randomness in the outputs."""
    tools: Annotated[
        Optional[List[ChatCompletionTool]],
        Field(
            default=None,
            description="A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported. ",
        ),
    ] = None
    """A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported. """
    top_p: Annotated[
        Optional[float],
        Field(
            default=1,
            description="An alternative to temperature for nucleus sampling; 1.0 includes all tokens.",
        ),
    ] = 1
    """An alternative to temperature for nucleus sampling; 1.0 includes all tokens."""

    model_config = ConfigDict(extra="ignore")


class CreateEvalCompletionsRunDataSource(BaseModel):
    """A CompletionsRunDataSource object describing a model sampling configuration."""

    input_messages: Annotated[
        Optional[Union[TemplateInputMessages, ItemReferenceInputMessages]],
        Field(
            default=None,
            description="Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace.",
        ),
    ] = None
    """Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace."""
    model: Annotated[
        Optional[str],
        Field(
            default=None,
            description='The name of the model to use for generating completions (e.g. "o3-mini").',
        ),
    ] = None
    """The name of the model to use for generating completions (e.g. \"o3-mini\")."""
    sampling_params: Annotated[Optional[SamplingParams], Field(default=None)] = None
    source: Annotated[
        Union[
            EvalJsonlFileContentSource,
            EvalJsonlFileIdSource,
            EvalStoredCompletionsSource,
        ],
        Field(
            ...,
            description="Determines what populates the `item` namespace in this run's data source.",
        ),
    ]
    """Determines what populates the `item` namespace in this run's data source."""
    type: Annotated[
        Literal["completions"],
        Field(
            default="completions",
            description="The type of run data source. Always `completions`.",
        ),
    ]
    """The type of run data source. Always `completions`."""

    model_config = ConfigDict(extra="ignore")


CreateEvalCompletionsRunDataSource.model_rebuild()
